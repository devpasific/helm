apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssb-idm-service.fullname" . }}-test-connection"
  labels:
    {{- include "ssb-idm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssb-idm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
