apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssb-kiosk-fe.fullname" . }}-test-connection"
  labels:
    {{- include "ssb-kiosk-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssb-kiosk-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
