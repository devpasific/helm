apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-portal-service.fullname" . }}
  labels:
    {{- include "ssb-portal-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-portal-service.selectorLabels" . | nindent 4 }}
