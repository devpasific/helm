apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-open-account-individu-service.fullname" . }}
  labels:
    {{- include "ssb-open-account-individu-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-open-account-individu-service.selectorLabels" . | nindent 4 }}
