apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssb-open-account-individu-service.fullname" . }}-test-connection"
  labels:
    {{- include "ssb-open-account-individu-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssb-open-account-individu-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
