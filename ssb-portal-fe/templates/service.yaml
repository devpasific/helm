apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-portal-fe.fullname" . }}
  labels:
    {{- include "ssb-portal-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-portal-fe.selectorLabels" . | nindent 4 }}
