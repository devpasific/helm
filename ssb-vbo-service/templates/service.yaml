apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-vbo-service.fullname" . }}
  labels:
    {{- include "ssb-vbo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-vbo-service.selectorLabels" . | nindent 4 }}
