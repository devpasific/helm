apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-cif-maintenance-service.fullname" . }}
  labels:
    {{- include "ssb-cif-maintenance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-cif-maintenance-service.selectorLabels" . | nindent 4 }}
