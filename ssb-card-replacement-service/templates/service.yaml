apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-card-replacement-service.fullname" . }}
  labels:
    {{- include "ssb-card-replacement-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-card-replacement-service.selectorLabels" . | nindent 4 }}
