apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssb-card-replacement-service.fullname" . }}-test-connection"
  labels:
    {{- include "ssb-card-replacement-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssb-card-replacement-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
