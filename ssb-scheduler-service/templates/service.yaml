apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-scheduler-service.fullname" . }}
  labels:
    {{- include "ssb-scheduler-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-scheduler-service.selectorLabels" . | nindent 4 }}
