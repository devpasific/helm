apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-product-service.fullname" . }}
  labels:
    {{- include "ssb-product-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-product-service.selectorLabels" . | nindent 4 }}
