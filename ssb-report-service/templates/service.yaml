apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-report-service.fullname" . }}
  labels:
    {{- include "ssb-report-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-report-service.selectorLabels" . | nindent 4 }}
