apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-gateway-service.fullname" . }}
  labels:
    {{- include "ssb-gateway-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-gateway-service.selectorLabels" . | nindent 4 }}
