apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-gateway-gcs.fullname" . }}
  labels:
    {{- include "ssb-gateway-gcs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-gateway-gcs.selectorLabels" . | nindent 4 }}
