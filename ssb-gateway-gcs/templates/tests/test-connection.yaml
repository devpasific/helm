apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssb-gateway-gcs.fullname" . }}-test-connection"
  labels:
    {{- include "ssb-gateway-gcs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssb-gateway-gcs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
