apiVersion: v1
kind: Service
metadata:
  name: {{ include "esb-dukcapil-service.fullname" . }}
  labels:
    {{- include "esb-dukcapil-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esb-dukcapil-service.selectorLabels" . | nindent 4 }}
