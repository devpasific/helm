apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esb-customer-validate-service.fullname" . }}-test-connection"
  labels:
    {{- include "esb-customer-validate-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esb-customer-validate-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
