apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-ktp-reader-service.fullname" . }}
  labels:
    {{- include "ssb-ktp-reader-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-ktp-reader-service.selectorLabels" . | nindent 4 }}
