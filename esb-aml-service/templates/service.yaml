apiVersion: v1
kind: Service
metadata:
  name: {{ include "esb-aml-service.fullname" . }}
  labels:
    {{- include "esb-aml-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esb-aml-service.selectorLabels" . | nindent 4 }}
