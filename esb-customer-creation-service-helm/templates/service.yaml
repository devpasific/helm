apiVersion: v1
kind: Service
metadata:
  name: {{ include "esb-customer-creation-service.fullname" . }}
  labels:
    {{- include "esb-customer-creation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esb-customer-creation-service.selectorLabels" . | nindent 4 }}
