apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esb-customer-creation-service.fullname" . }}-test-connection"
  labels:
    {{- include "esb-customer-creation-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esb-customer-creation-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
