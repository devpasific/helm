apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-vbo-fe.fullname" . }}
  labels:
    {{- include "ssb-vbo-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-vbo-fe.selectorLabels" . | nindent 4 }}
