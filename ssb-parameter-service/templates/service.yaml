apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssb-parameter-service.fullname" . }}
  labels:
    {{- include "ssb-parameter-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssb-parameter-service.selectorLabels" . | nindent 4 }}
